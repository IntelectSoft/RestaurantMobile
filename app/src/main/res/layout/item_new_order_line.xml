<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/linearLayout4"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="@dimen/_6sdp"
    android:background="@drawable/line_preview_background">

    <TextView
        android:id="@+id/textNameAssortment"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="4dp"
        android:gravity="center_vertical"
        android:text="name"
        android:textAppearance="?android:attr/textAppearanceListItemSmall"
        android:textSize="22sp"
        app:layout_constraintEnd_toStartOf="@+id/textCount"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/textCount"
        android:layout_width="55dp"
        android:layout_height="wrap_content"
        android:layout_marginEnd="8dp"
        android:minWidth="@dimen/_30sdp"
        android:text="count"
        android:textColor="@color/counts"
        android:textSize="@dimen/_12ssp"
        android:gravity="right"
        app:layout_constraintBottom_toBottomOf="@+id/textNameAssortment"
        app:layout_constraintEnd_toStartOf="@+id/textPrice"
        app:layout_constraintTop_toTopOf="@+id/textNameAssortment" />

    <TextView
        android:id="@+id/textPrice"
        android:layout_width="50dp"
        android:layout_height="wrap_content"
        android:gravity="right"
        android:minWidth="@dimen/_30sdp"
        android:text="price"
        android:textColor="@android:color/holo_red_light"
        android:textSize="@dimen/_12ssp"
        app:layout_constraintBottom_toBottomOf="@+id/textNameAssortment"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="@+id/textNameAssortment" />


</androidx.constraintlayout.widget.ConstraintLayout>
